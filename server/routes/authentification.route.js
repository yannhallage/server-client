const express = require('express')
const route_authentifcation = express.Router();

// ici on gere les Auths

const  GetAutthentification = async (req,res) =>{
    try {
        const {id} = parmas
    }
}
